name: Template Parameter true_dpT_lp_genie
isEnabled: false

printDialSetsSummary: true

nominalStepSize: 0.1
useOnlyOneParameterPerEvent: true

dialSetDefinitions:
  - name: Template Parameter true_dpT_lp_genie  dialSetDefinitions
    # 'applyOnDataSets' can take a list or a wildcard
    applyOnDataSets: ["*"]
    parametersBinningPath: "${GUNDAM_CONFIG_DIR}/binnings/binning_true_dpT.txt"
    applyCondition: "([category]==0) || ([category]==1) || ([category]==3) || ([category]==4)"
    printDialsSummary: true

parameterLimits:
  minValue: 0.
  maxValue: 3.0
